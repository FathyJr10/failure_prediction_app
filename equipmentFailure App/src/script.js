function submitForm() {
    // Get the input element
    var inputElement = document.getElementById('operatingHours');

    // Get the input value
    var inputValue = inputElement.value;
    
    // Print the input value to the console
    console.log(inputValue);
}
